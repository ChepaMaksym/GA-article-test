# USA-001 compact failure-evidence bundle

This directory preserves the compact evidence needed to audit the failed
USA-001 reproduction after deletion of the failed candidate workspace.
It is not a successful reproducibility package.

## Candidate cleanup

- Deleted candidate directory:
  `candidate_02_degroot_adaptive_ga`
- Deletion date: `2026-07-30`
- Files removed: `202`
- Bytes removed: `457181299`
- Existence check after deletion: `false`
- Pre-deletion file inventory:
  `candidate_file_manifest.csv`
- Inventory SHA-256:
  `2add8ec03e683b3091a0151840adcd0ee39b3cb6749f07a3745a3503a645c1a0`

The deletion used a direct native filesystem removal after resolving the
exact workspace child and checking the external failure records. It did
not deliberately place the folder in the Windows Recycle Bin. OneDrive
may provide separate server-side recovery depending on synchronization
and retention state; that was not tested or assumed.

## Preserved evidence

The bundle contains:

- the primary and Q1/Q2/Q3 ten-run ledgers;
- the primary and Q1/Q2/Q3 published-result verification tables;
- the three diagnostic summaries and signed provenance MAT files;
- the frozen decision-variable and instance manifests;
- the published target and sensitivity rows;
- the source manifest, source audit, and experiment specification;
- the diagnostic-harness invalidation log;
- the complete pre-deletion relative-path, byte-size, and SHA-256
  inventory.

Large generation/event ledgers, run caches, copied upstream source data,
and implementation files were not retained. Their pre-deletion hashes
remain in the detailed failure record or the full candidate inventory.

## Outcome

The paper-faithful primary and all three permitted diagnostics failed the
published P2/P4 endpoints. In every profile, run 8 remained nonzero
through 100000 generations at agent 18, time 2. Q2 and Q3 matched the
published recovery statistic in isolation, but neither resolved the
required exact-fit endpoint.

The 46-test diagnostic suite passed with zero failures and zero
incomplete tests. A line reading `Primary published-result verification:
PASS` in that suite is generated by a synthetic pass-logic fixture; it is
not the real primary or Q3 outcome.

No fixed-GA comparison, ablation, 18-sheet workbook, or success
reproducibility package was generated because the published-result entry
gate never passed.
