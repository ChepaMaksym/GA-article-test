# EU26-01 eligibility re-audit — 2026-08-07

Decision: **retain `conditional_noneligible`; authorize only
`ARCHIVE_AND_FORMULA_VALIDATION_ONLY`.**

The outcome-blind cell is Table 1 Algorithm 2 TwoRate using hypervolume on
100-bit OneMinMax with `lambda=10`, `r_init=1` and 100 runs. Published targets
are mean `61624` full-front completion function evaluations and population
variance `3.271e+08`.

The exact CC-BY-4.0 Zenodo archive `csv.zip` is 173,556,521 bytes with SHA-256
`2d5d3491c23e68bdd6d3c9c9dedfae67c805fa0588a3d443912c0b12c348fe4a`.
The sole admitted member is
`csv/om/TwoRateL10P1HVOneMaxD100.csv`, 102,227 bytes, SHA-256
`75a7788105ac9b73f6f4a1baa2a84d5ffb0d13e736da70115319e9eb62eda154`.
Authenticated recalculation yields 100/100 complete runs, sum `6162378`, exact
mean `61623.78`, and exact population variance `327085104.6716`. These round
to both published values under the frozen rules.

This establishes an archive-to-table recalculation, not source-native replay.
Literal one-based Algorithm 2 with `i < floor(lambda/2)` gives 4 low-rate and
6 high-rate children at `lambda=10`; prose says half/half and source uses 5/5.
The paper does not define `arg max` ties, whereas source uses first maximum.
The verification freezes source/prose 5/5 plus first maximum and labels that
choice explicitly.

Paper `flip_l` treats `l` as the number of flipped bits. The source calls an
unpinned IOHexperimenter integer-vector sampler instead of its own
distinct-index helper; available dependency code samples with replacement,
so repeated indices may cancel. The clean-room tape rejects duplicates and is
paper-faithful on this formula, not proof of source trajectory equivalence.

The source calls the IOHexperimenter RNG seed `10` once before 100 sequential
problem resets. It supplies no per-run seed/RNG-state ledger, so G9 changes
from pass to unresolved. A clean checkout also has absolute `include` and
`external` symlinks into a developer IOHexperimenter tree, no pinned
dependency revision, no complete build lock and no code license. The brief
historical ELF predates later fixes and cannot be bound to the archived table.
G10 therefore remains unresolved.

Allowed positive labels are narrowly `PASS_ARCHIVE_EXACT`, `PASS_FORMULA` and
`PASS_PORTABILITY`. H5 additionally requires a separately acquired GitHub API
record binding the expected repository/workflow, head/run/attempt, artifact
identity, raw GitHub4 report bytes and internal report digest; three matching
self-asserted profiles remain unauthenticated. None clears
`BLOCKED_SOURCE_NATIVE_REPLAY`; `PASS_FULL` is forbidden. The AGSEMO Table 2
row is outside this candidate contract.
