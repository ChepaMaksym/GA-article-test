name: EU26-21 legacy sequential OLD vs Hybrid 1 comparison v2 (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Explain retirement
        run: |
          echo 'This sequential v2 workflow is retained only for provenance.'
          echo 'It was superseded by the seed-isolated matrix workflow.'
          echo 'Use eu26-21-old-hybrid-30-matrix.yml and the strict audit workflow.'
