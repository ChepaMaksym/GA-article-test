name: EU26-21 legacy OLD vs Hybrid 1 comparison v1 (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-24.04
    steps:
      - name: Explain retirement
        run: |
          echo 'This v1 workflow is retained only for provenance.'
          echo 'It used asymmetric target-attainment semantics and is not a valid final experiment.'
          echo 'Use eu26-21-old-hybrid-30-matrix.yml and the strict audit workflow instead.'
